{
  "buildCommand": "pnpm vite build",
  "outputDirectory": "dist",
  "functions": {
    "api/**/*.{ts,js}": {
      "maxDuration": 120
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}