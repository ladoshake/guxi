{
  "buildCommand": "pnpm vite build",
  "outputDirectory": "dist",
  "functions": {
    "api/**/*.{ts,js}": {
      "runtime": "nodejs20.x",
      "maxDuration": 120
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}